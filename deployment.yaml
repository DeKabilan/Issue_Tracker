apiVersion: apps/v1
kind: Deployment
metadata:
 name: issuetracker
 labels:
   app: issue-tracker
spec:
 replicas: 3
 selector:
   matchLabels:
     app: issue-tracker
 template:
   metadata:
     labels:
       app: issuetracker
   spec:
     containers:
     - name: issue-tracker
       image: jp.icr.io/issuetracker/issuetracker:1.0
       ports:
       - containerPort: 5000


